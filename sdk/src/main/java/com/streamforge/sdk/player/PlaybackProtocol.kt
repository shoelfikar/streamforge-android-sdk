package com.streamforge.sdk.player

/** Supported streaming protocols. */
enum class PlaybackProtocol {
    /** HTTP Live Streaming (Apple HLS). */
    HLS,
    /** Dynamic Adaptive Streaming over HTTP (MPEG-DASH). */
    DASH
}
